name: CotaPeca Sprint 2 CI

on:
  push:
    branches: [cotapeca-recovery]
    paths:
      - 'cotapeca-recovered/**'
      - '.github/workflows/cotapeca-ci.yml'
  pull_request:
    branches: [cotapeca-recovery]
    paths:
      - 'cotapeca-recovered/**'
      - '.github/workflows/cotapeca-ci.yml'
  workflow_dispatch:

jobs:
  gates:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: cotapeca-recovered
    env:
      NEXT_PUBLIC_SUPABASE_URL: https://aenmxjmrhancvqgpunhk.supabase.co
      NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY: sb_publishable_UofCGoeTB-E3vJyWJXUepQ_U5Ka5tcM
      NEXT_PUBLIC_E2E_MODE: 'true'
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: cotapeca-recovered/package-lock.json
      - name: Install
        run: npm ci --no-audit --no-fund
      - name: Typecheck
        run: npm run typecheck
      - name: Lint
        run: npm run lint
      - name: Test
        run: npm test
      - name: Build
        run: npm run build
